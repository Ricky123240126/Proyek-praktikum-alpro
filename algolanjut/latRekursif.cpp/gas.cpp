#include <iostream>
using namespace std;

int angka[nilai] = {4,6,1,7,9};
int panjangArray = sizeof(angka) / sizeof(angka[0]);

int angkaTerbesar(int angka[nilai])
{
    if (angka[nilai] = 1)
    {
        return angka[0];
    }
    else 
    {
        
    }
}

int main()
{
    cout << "angka tertinggi = " << angkaTerbesar(a);
}